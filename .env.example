# PaintForge Docker Configuration

# User/Group IDs (linuxserver.io convention)
PUID=1000
PGID=1000

# Timezone (IANA format)
TZ=America/New_York

# Database path (inside container)
DB_PATH=/app/data/paintforge.db
