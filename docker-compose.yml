services:
  paintforge:
    image: ghcr.io/cruv/paintforge:latest
    ports:
      - "8081:80"
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/New_York
    restart: unless-stopped
