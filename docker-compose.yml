services:
  paintforge:
    image: ghcr.io/cruv/paintforge:latest
    ports:
      - "8081:80"
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/New_York
      - DB_PATH=/app/data/paintforge.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
